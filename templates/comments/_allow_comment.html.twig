<form method="post" action="{{ path('app_comments_allow', {'id': comment.id}) }}" onsubmit="return confirm('Vous étes certain de vouloir autoriser ce commentaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn btn-primary">Autoriser</button>
</form>