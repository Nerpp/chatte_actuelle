<form method="post" action="{{ path('app_edito_delete', {'id': edito.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ edito.id) }}">
    <button class="btn">Delete</button>
</form>
