{% extends 'base.html.twig' %}

{% block title %}Edito{% endblock %}

{% block body %}
    <h1>Edito</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edito.id }}</td>
            </tr>
            <tr>
                <th>Edito</th>
                <td>{{ edito.edito }}</td>
            </tr>
            <tr>
                <th>Draft</th>
                <td>{{ edito.draft ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ edito.publishedAt ? edito.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_edito_index') }}">back to list</a>

    <a href="{{ path('app_edito_edit', {'id': edito.id}) }}">edit</a>

    {{ include('edito/_delete_form.html.twig') }}
{% endblock %}
