<form method="post" action="{{ path('app_articles_delete', {'slug': article.slug}) }}" onsubmit="return confirm('la suppression de l\'article est définitive');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
